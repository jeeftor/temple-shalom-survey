name       = "temple-shalom-survey"
account_id = "ab1a1103db401de7e9e722787fde9c0a"

# Cloudflare Pages — static site deployment
# Manual deploy: wrangler pages deploy . --project-name temple-shalom-survey
# Auto deploy:   push to master branch (via GitHub integration or GH Actions)
#
# CF Pages build settings (set in dashboard or used by native integration):
#   Build command:    bash build.sh
#   Build output dir: /
